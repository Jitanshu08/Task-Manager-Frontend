import React from 'react';
import Navbar from '../components/Navbar';

const Settings = () => {
  return (
    <div className="settings-page">
      <Navbar />
      <h1>Settings</h1>
      {/* Add form for updating user info (name, email, password) */}
    </div>
  );
};

export default Settings;
